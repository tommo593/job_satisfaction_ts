const Button = () => {
  return (
    <div>
      <button className="bg-button_bg hover:bg-slider_bg transition duration-300 px-8 py-2 rounded text-lg mt-8">
        Button
      </button>
    </div>
  );
};

export default Button;
